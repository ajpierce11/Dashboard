# Copy this file to .env and fill in real values.
# .env is gitignored — never commit real keys.

# AbbVie ILIAD gateway API key (required for AI Assistant tab and index building)
ILIAD_API_KEY=

# Optional: override default file/folder locations.
# Defaults in config.py resolve relative to the repo root if unset.
# TESTING_FILE_PATH=C:\path\to\testing fpt files.xlsx
# LIBRARY_PATH=C:\path\to\Library
